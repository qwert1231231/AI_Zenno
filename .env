# Environment variables
# Fill these with your keys/config when deploying

# OpenAI (or similar) API key
OPENAI_API_KEY=""

# ChromaDB path or connection string (if used)
CHROMA_DB_PATH="./chroma_db"

# FastAPI server settings
HOST=127.0.0.1
PORT=8000

# Secret key for auth (change in production)
SECRET_KEY="change-this-to-a-secret"
